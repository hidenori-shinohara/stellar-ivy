#lang ivy1.7

include node_module
include network_module

type value
type id

instance node : node_module(value, id)
instance network : network_module(node, value)

# IVy will play with these two actions
# and see if any invariants ever get violated.
export network.enqueue_vote
export network.deliver_vote
